name: Curbside
options:
  minimumXcodeGenVersion: 2.38.0
  bundleIdPrefix: com.nulljosh
settings:
  base:
    # Must live under settings.base -- xcodegen silently drops DEVELOPMENT_TEAM
    # if you put it on a target.
    DEVELOPMENT_TEAM: QMM486NPYC
    SWIFT_VERSION: "6.0"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
targets:
  Curbside:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Sources
        excludes: ["iOS/**"]
      - path: Resources
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.nulljosh.curbside
        PRODUCT_NAME: Curbside
        ENABLE_HARDENED_RUNTIME: YES
        # Missing LSApplicationCategoryType is the real cause of ITMS-90242.
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_ENTITLEMENTS: Resources/Curbside.entitlements
  Curbside-iOS:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Sources/Models
      - path: Sources/Views
      - path: Sources/iOS
      - path: Resources
        buildPhase: resources
        excludes: ["Curbside.entitlements"]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.nulljosh.curbside.ios
        PRODUCT_NAME: Curbside
        TARGETED_DEVICE_FAMILY: "1,2"
        # Hand-written plist purely because UILaunchScreen is a dict and no
        # INFOPLIST_KEY_* can express it; without it the app runs letterboxed.
        INFOPLIST_FILE: Curbside-iOS-Info.plist
        CODE_SIGN_ENTITLEMENTS: Curbside-iOS.entitlements
  CurbsideTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests
      - path: Resources/search-fixture.json
        buildPhase: resources
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: Curbside
